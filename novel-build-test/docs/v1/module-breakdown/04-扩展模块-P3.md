# P3 扩展模块 — 增强功能

> 优先级: P3 (Phase 4)
> 说明: 增强用户体验和集成功能

---

## 1. AI 智能体模块群

### 1.1 需求分析 (agent-requirements)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 发起分析 | ACTION | 选择PRD发起需求分析 |
| 分析状态 | READ | 查看分析进度 |
| 分析结果 | READ | 查看结构化输出 |
| 保存结果 | CREATE | 保存分析结果到需求 |

#### API 接口
```
POST   /api/v1/agents/requirements/analyze    # 发起需求分析
GET    /api/v1/agents/requirements/:id/status # 分析状态
GET    /api/v1/agents/requirements/:id/result # 分析结果
POST   /api/v1/agents/requirements/:id/save   # 保存结果
```

---

### 1.2 测试架构 (agent-architect)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 发起设计 | ACTION | 基于需求发起架构设计 |
| 设计状态 | READ | 查看设计进度 |
| 设计结果 | READ | 查看架构方案 |

#### API 接口
```
POST   /api/v1/agents/architect/design        # 发起架构设计
GET    /api/v1/agents/architect/:id/status    # 设计状态
GET    /api/v1/agents/architect/:id/result    # 设计结果
```

---

### 1.3 测试设计 (agent-designer)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 发起设计 | ACTION | 发起测试场景设计 |
| 设计状态 | READ | 查看设计进度 |
| 设计结果 | READ | 查看场景大纲 |

#### API 接口
```
POST   /api/v1/agents/designer/design         # 发起场景设计
GET    /api/v1/agents/designer/:id/status     # 设计状态
GET    /api/v1/agents/designer/:id/result     # 设计结果
```

---

### 1.4 用例编写 (agent-casewriter)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 发起编写 | ACTION | 发起用例生成 |
| 编写状态 | READ | 查看编写进度 |
| 用例结果 | READ | 查看生成的用例 |
| 导入用例 | CREATE | 将生成的用例导入系统 |

#### API 接口
```
POST   /api/v1/agents/casewriter/generate     # 发起用例生成
GET    /api/v1/agents/casewriter/:id/status   # 编写状态
GET    /api/v1/agents/casewriter/:id/result   # 用例结果
POST   /api/v1/agents/casewriter/:id/import   # 导入用例
```

---

### 1.5 执行分析 (agent-execution)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 发起分析 | ACTION | 发起执行结果分析 |
| 分析状态 | READ | 查看分析进度 |
| 分析结果 | READ | 查看失败根因和建议 |

#### API 接口
```
POST   /api/v1/agents/execution/analyze       # 发起执行分析
GET    /api/v1/agents/execution/:id/status    # 分析状态
GET    /api/v1/agents/execution/:id/result    # 分析结果
```

---

### 1.6 质量审计 (agent-quality)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 发起审计 | ACTION | 发起质量审计 |
| 审计状态 | READ | 查看审计进度 |
| 审计结果 | READ | 查看质量评分和建议 |

#### API 接口
```
POST   /api/v1/agents/quality/audit           # 发起质量审计
GET    /api/v1/agents/quality/:id/status      # 审计状态
GET    /api/v1/agents/quality/:id/result      # 审计结果
```

---

### 1.7 成本优化 (agent-cost)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 成本概览 | READ | 查看整体成本统计 |
| 成本趋势 | READ | 查看成本变化趋势 |
| 模型分布 | READ | 查看各模型使用占比 |
| 优化建议 | READ | 查看成本优化建议 |

#### API 接口
```
GET    /api/v1/agents/cost/overview           # 成本概览
GET    /api/v1/agents/cost/trend              # 成本趋势
GET    /api/v1/agents/cost/distribution       # 模型分布
GET    /api/v1/agents/cost/suggestions        # 优化建议
```

---

### 1.8 辩论引擎 (agent-debate)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 发起辩论 | ACTION | 创建辩论话题并发起 |
| 辩论状态 | READ | 查看辩论进度 |
| 辩论结果 | READ | 查看各轮辩论和共识 |
| 辩论记录列表 | READ | 查询历史辩论 |
| 记录详情 | READ | 查看历史辩论详情 |

#### API 接口
```
POST   /api/v1/agents/debate/launch           # 发起辩论
GET    /api/v1/agents/debate/:id/status       # 辩论状态
GET    /api/v1/agents/debate/:id/result       # 辩论结果
GET    /api/v1/agents/debate/records          # 辩论记录列表
GET    /api/v1/agents/debate/records/:id      # 记录详情
```

#### 数据模型
```sql
CREATE TABLE debate_records (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    topic           TEXT NOT NULL,
    models          JSON NOT NULL,   -- 参与辩论的模型
    rounds          JSON,            -- 各轮辩论内容
    consensus       TEXT,            -- 共识结论
    status          TEXT DEFAULT 'running',  -- running/completed
    created_by      INTEGER REFERENCES users(id),
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    finished_at     DATETIME
);
```

---

## 2. AI 应用模块群

### 2.1 AI 聊天室 (ai-chatroom)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 创建会话 | CREATE | 创建新的对话会话 |
| 发送消息 | CREATE | 发送用户消息 |
| 获取回复 | READ | 获取AI回复（流式） |
| 会话列表 | READ | 查询历史会话 |
| 会话详情 | READ | 查看会话消息历史 |
| 删除会话 | DELETE | 删除会话 |

#### API 接口
```
POST   /api/v1/chat/sessions                 # 创建会话
POST   /api/v1/chat/sessions/:id/messages    # 发送消息
GET    /api/v1/chat/sessions/:id/messages    # 获取消息历史
GET    /api/v1/chat/sessions                  # 会话列表
DELETE /api/v1/chat/sessions/:id              # 删除会话
```

#### 数据模型
```sql
CREATE TABLE chat_sessions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    title           TEXT,
    model           TEXT,           -- 使用的模型
    created_by      INTEGER REFERENCES users(id),
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE chat_messages (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id      INTEGER NOT NULL REFERENCES chat_sessions(id),
    role            TEXT NOT NULL,  -- user/assistant/system
    content         TEXT NOT NULL,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 2.2 AI 数据库调优 (ai-db-tuning)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 数据库连接配置 | CRUD | 配置数据库连接 |
| 慢查询检测 | ACTION | 执行慢查询分析 |
| 索引优化建议 | READ | 查看索引优化建议 |
| 连接池分析 | READ | 查看连接池使用情况 |

#### API 接口
```
POST   /api/v1/ai-db/connect                 # 测试数据库连接
POST   /api/v1/ai-db/slow-queries            # 检测慢查询
GET    /api/v1/ai-db/index-suggestions       # 索引优化建议
GET    /api/v1/ai-db/connection-pool         # 连接池分析
```

---

### 2.3 AI 助手 (ai-assistant)

#### 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 快捷跳转 | READ | 获取模块快捷跳转列表 |
| 项目总览 | READ | 获取项目整体状态 |
| 智能问答 | ACTION | 与AI进行测试相关对话 |

#### API 接口
```
GET    /api/v1/ai-assistant/quick-actions     # 快捷跳转列表
GET    /api/v1/ai-assistant/overview          # 项目总览
POST   /api/v1/ai-assistant/chat              # 智能问答
```

---

## 3. Hermes 智能体配置 (hermes-config)

### 3.1 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 会话列表 | READ | 列出活跃对话会话 |
| 会话详情 | READ | 查看会话详细信息 |
| 消息读取 | READ | 读取会话消息 |
| 消息发送 | CREATE | 发送消息到会话 |
| 通道列表 | READ | 查询已配置的通道 |
| 创建通道 | CREATE | 新增消息通道 |
| 编辑通道 | UPDATE | 修改通道配置 |
| 删除通道 | DELETE | 删除通道 |
| 事件轮询 | READ | 获取新事件 |
| 等待事件 | READ | 长轮询等待事件 |
| 附件获取 | READ | 获取消息附件 |
| 权限列表 | READ | 查询待审批请求 |
| 权限响应 | UPDATE | 响应审批请求 |

### 3.2 API 接口
```
GET    /api/v1/hermes/conversations           # 会话列表
GET    /api/v1/hermes/conversations/:key      # 会话详情
GET    /api/v1/hermes/messages/:session_key   # 读取消息
POST   /api/v1/hermes/messages                # 发送消息

GET    /api/v1/hermes/channels                # 通道列表
POST   /api/v1/hermes/channels                # 创建通道
PUT    /api/v1/hermes/channels/:id            # 更新通道
DELETE /api/v1/hermes/channels/:id            # 删除通道

GET    /api/v1/hermes/events/poll             # 事件轮询
GET    /api/v1/hermes/events/wait             # 等待事件

GET    /api/v1/hermes/attachments/:message_id # 获取附件

GET    /api/v1/hermes/permissions             # 权限列表
POST   /api/v1/hermes/permissions/:id/respond # 响应审批
```

### 3.3 数据模型
```sql
CREATE TABLE hermes_channels (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT NOT NULL,
    platform        TEXT NOT NULL,   -- telegram/discord/slack/custom
    config          JSON NOT NULL,   -- 平台配置
    status          TEXT DEFAULT 'active',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE hermes_permissions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    request_type    TEXT NOT NULL,
    request_data    JSON NOT NULL,
    status          TEXT DEFAULT 'pending',  -- pending/approved/denied
    responded_at    DATETIME,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 4. Skills 技能中心 (skills-center)

### 4.1 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 技能列表 | READ | 查询已注册技能 |
| 技能详情 | READ | 查看技能配置 |
| 注册技能 | CREATE | 注册新技能 |
| 编辑技能 | UPDATE | 修改技能配置 |
| 删除技能 | DELETE | 删除技能 |
| MCP工具列表 | READ | 查询MCP工具 |
| 注册MCP工具 | CREATE | 注册新MCP工具 |
| 编辑MCP工具 | UPDATE | 修改MCP工具配置 |
| 删除MCP工具 | DELETE | 删除MCP工具 |
| 权限配置 | READ/UPDATE | 查看/配置Agent工具权限 |
| 技能市场列表 | READ | 查询预置技能包 |
| 启用技能包 | ACTION | 一键启用技能包 |
| 技能测试 | ACTION | 测试技能执行效果 |
| 调用日志 | READ | 查询技能调用记录 |

### 4.2 API 接口
```
GET    /api/v1/skills                        # 技能列表
GET    /api/v1/skills/:id                    # 技能详情
POST   /api/v1/skills                        # 注册技能
PUT    /api/v1/skills/:id                    # 更新技能
DELETE /api/v1/skills/:id                    # 删除技能

GET    /api/v1/skills/mcp                    # MCP工具列表
POST   /api/v1/skills/mcp                    # 注册MCP工具
PUT    /api/v1/skills/mcp/:id                # 更新MCP工具
DELETE /api/v1/skills/mcp/:id                # 删除MCP工具

GET    /api/v1/skills/permissions            # 权限配置
PUT    /api/v1/skills/permissions            # 更新权限配置

GET    /api/v1/skills/market                 # 技能市场列表
POST   /api/v1/skills/market/:id/enable      # 启用技能包

POST   /api/v1/skills/:id/test               # 技能测试
GET    /api/v1/skills/logs                   # 调用日志
```

### 4.3 数据模型
```sql
CREATE TABLE skills (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT NOT NULL,
    type            TEXT NOT NULL,   -- mcp/internal/custom
    description     TEXT,
    config          JSON NOT NULL,
    status          TEXT DEFAULT 'active',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE skill_permissions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_key       TEXT NOT NULL,
    skill_id        INTEGER NOT NULL REFERENCES skills(id),
    enabled         BOOLEAN DEFAULT TRUE,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE skill_logs (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    skill_id        INTEGER NOT NULL REFERENCES skills(id),
    agent_key       TEXT,
    input           JSON,
    output          JSON,
    duration_ms     INTEGER,
    status          TEXT,           -- success/failed
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 5. 集成与通知 (integration)

### 5.1 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| CI/CD配置列表 | READ | 查询CI/CD集成配置 |
| 创建CI/CD配置 | CREATE | 新增Webhook配置 |
| 编辑CI/CD配置 | UPDATE | 修改配置 |
| 删除CI/CD配置 | DELETE | 删除配置 |
| 通知渠道列表 | READ | 查询通知渠道 |
| 创建通知渠道 | CREATE | 新增通知渠道 |
| 编辑通知渠道 | UPDATE | 修改渠道配置 |
| 删除通知渠道 | DELETE | 删除渠道 |
| 测试通知 | ACTION | 发送测试通知 |
| 外部工具列表 | READ | 查询外部工具配置 |
| 创建外部工具 | CREATE | 新增工具配置 |
| 编辑外部工具 | UPDATE | 修改工具配置 |
| 删除外部工具 | DELETE | 删除工具配置 |

### 5.2 API 接口
```
GET    /api/v1/integration/cicd               # CI/CD配置列表
POST   /api/v1/integration/cicd               # 创建配置
PUT    /api/v1/integration/cicd/:id           # 更新配置
DELETE /api/v1/integration/cicd/:id           # 删除配置

GET    /api/v1/integration/notifications      # 通知渠道列表
POST   /api/v1/integration/notifications      # 创建渠道
PUT    /api/v1/integration/notifications/:id  # 更新渠道
DELETE /api/v1/integration/notifications/:id  # 删除渠道
POST   /api/v1/integration/notifications/:id/test  # 测试通知

GET    /api/v1/integration/tools              # 外部工具列表
POST   /api/v1/integration/tools              # 创建工具
PUT    /api/v1/integration/tools/:id          # 更新工具
DELETE /api/v1/integration/tools/:id          # 删除工具
```

### 5.3 数据模型
```sql
CREATE TABLE cicd_configs (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id      INTEGER NOT NULL REFERENCES projects(id),
    name            TEXT NOT NULL,
    type            TEXT NOT NULL,   -- jenkins/github/gitlab
    webhook_url     TEXT NOT NULL,
    secret          TEXT,
    events          JSON,            -- 触发事件
    status          TEXT DEFAULT 'active',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE notification_channels (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT NOT NULL,
    type            TEXT NOT NULL,   -- email/dingtalk/feishu/slack
    config          JSON NOT NULL,
    status          TEXT DEFAULT 'active',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE external_tools (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT NOT NULL,
    type            TEXT NOT NULL,   -- jira/git/zentao
    config          JSON NOT NULL,
    status          TEXT DEFAULT 'active',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 6. MCP 工具集成 (mcp)

### 6.1 功能清单
| 功能 | 类型 | 说明 |
|------|------|------|
| 工具列表 | READ | 列出可用MCP工具 |
| 工具详情 | READ | 查看工具配置 |
| 执行工具 | ACTION | 调用指定工具 |

### 6.2 API 接口
```
GET    /api/v1/mcp/tools                     # 工具列表
GET    /api/v1/mcp/tools/:name               # 工具详情
POST   /api/v1/mcp/tools/:name/execute       # 执行工具
```

### 6.3 数据模型
```sql
-- MCP工具通过配置文件管理，无需独立数据库表
-- 配置存储在 mcp-config.json 中
```
